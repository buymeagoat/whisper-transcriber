# TODO: Copilot Evaluation
- [x] A1 Jobs API rejects authenticated uploads – resolved
	- Details: Adjusted auth dependency to honor JWT/cookie identity, retagged Docker Python base to 3.13 so `openai-whisper` dependencies build, and aligned `.env` runtime values with bundled SQLite + redis password for compose parity.
	- Fix:
		- Final Dockerfile targets `python:3.13-slim-bookworm` for builder/runtime; `.env` points `DATABASE_URL` to `/app/data/whisper.db` and syncs `REDIS_URL` credentials; JWT-based auth dependency already merged earlier.
	- Model: `pytest --override-ini addopts='' tests/test_api_flow.py`; manual docker smoke (home page, admin login, user registration via `/api/register`, WAV upload, container restart + job persistence).
	- Next Action: None.
