# Production environment variables template
# Populate via your secrets manager and copy to `.env.production` during deployment.

# Critical secrets (generate with `openssl rand -hex 64` and rotate regularly)
SECRET_KEY=
JWT_SECRET_KEY=
REDIS_PASSWORD=
ADMIN_BOOTSTRAP_PASSWORD=
ADMIN_METRICS_TOKEN=

# Database configuration
DATABASE_URL=
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_SSLMODE=prefer
POSTGRES_OPTIONS=

# Redis / Celery endpoints
REDIS_URL=
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

# Runtime configuration
HOST=0.0.0.0
PORT=8001
LOG_LEVEL=INFO
ENVIRONMENT=production
WHISPER_MODEL_DIR=/app/models
PYTHONUNBUFFERED=1
PYTHONDONTWRITEBYTECODE=1
