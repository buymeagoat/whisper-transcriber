# Production Environment Variables Template
# Copy this file to .env.production (never commit secrets) and populate values via your secrets manager.
# Rotate each secret using your vault's versioning or dynamic credential APIs before redeploying.

# SECRET_KEY: 64+ character application signing key (rotate alongside JWT secret).
SECRET_KEY=
# JWT_SECRET_KEY: Dedicated key for signing API tokens. Generate with `openssl rand -hex 64` and rotate quarterly.
JWT_SECRET_KEY=
# REDIS_PASSWORD: Strong password provisioned for the Redis instance. Rotate with Redis ACL tooling.
REDIS_PASSWORD=
# ADMIN_BOOTSTRAP_PASSWORD: One-time administrator bootstrap credential. Rotate immediately after first login.
ADMIN_BOOTSTRAP_PASSWORD=
# ADMIN_METRICS_TOKEN: Token for protected admin metrics endpoints. Issue via your secret manager with short TTL.
ADMIN_METRICS_TOKEN=

# DATABASE_URL: Production database DSN provided by your database platform or secret broker (e.g., AWS RDS IAM auth).
DATABASE_URL=
# POSTGRES_HOST: Primary PostgreSQL endpoint or load balancer address.
POSTGRES_HOST=
# POSTGRES_PORT: PostgreSQL port exposed by the managed database service (defaults to 5432).
POSTGRES_PORT=
# POSTGRES_DB: Database name dedicated to the Whisper Transcriber application.
POSTGRES_DB=
# POSTGRES_USER: Service account used for application connections (should enforce least privilege roles).
POSTGRES_USER=
# POSTGRES_PASSWORD: Credential for the PostgreSQL user. Leave blank when using IAM or passwordless auth.
POSTGRES_PASSWORD=
# POSTGRES_SSLMODE: SSL enforcement mode (e.g., require, verify-full) matching your security posture.
POSTGRES_SSLMODE=
# POSTGRES_OPTIONS: Optional libpq options string (e.g., -c statement_timeout=5000) encoded as part of the DSN.
POSTGRES_OPTIONS=
# REDIS_URL: Redis connection string with authentication (e.g., rediss://:password@host:6379/0).
REDIS_URL=

HOST=0.0.0.0
PORT=8001
LOG_LEVEL=INFO

ENVIRONMENT=production
WHISPER_MODEL_DIR=/app/models
